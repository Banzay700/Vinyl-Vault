import { motion } from 'framer-motion'
import { pageAnimation } from '../../utils/motionOptions'

const Home = () => {
  return <motion.div {...pageAnimation}>Home content</motion.div>
}

export default Home
